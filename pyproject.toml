[project]
name = "engagement-push-automation"
version = "0.1.0"
dependencies = [
    "requests",
    "beautifulsoup4",
    "flask",
    "google-generativeai",
    "python-dotenv",
    "gspread",
    "oauth2client"
]

[tool.vercel]
entrypoint = "automation_push.api.generate_push:handler"
